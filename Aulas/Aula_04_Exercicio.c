/*
1. Fazer um programa que cria um thread e espera por sua finalização. A thread criada deve conter um loop infinito. Use os comandos abaixo enquanto o programa está executando:
    # ps -aLf
    # top -H -p <pid>

2. Faça um programa  que declara uma variável global e cria (fork) um outro processo. O processo filho lê esta variável global e a altera. Imprima o valor desta vriável no processo pai, depois do término do filho.

3. Faça uma versão do programa anterior usando threads.

4. Faça um programa para somar os elementos de um vetor usando threads. Crie 4 threads.

*/

#include <stdio.h>

int main(int argc, char const *argv[])
{
    /* code */
    return 0;
}
